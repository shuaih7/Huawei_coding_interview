# -*- coding: utf-8 -*-

if __name__ == "__main__":
    while True:
        try:
            #print(sorted(input()))
            print("".join(sorted(input()))) # Use "join" method, easy join the string with a char list
        except:break